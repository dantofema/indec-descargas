node_modules/
dist/
.cache/
*.local
.DS_Store

# volcados intermedios del build (grandes, regenerables)
scripts/.cache/

# workspace de subagent-driven-development (scratch)
.superpowers/

# El backlog vive en su propio repo (~/Projects/backlog).
# docs/backlog es un symlink a ../../backlog/indec-descargas.
/docs/backlog
